import EmployeeDashboard from "@/components/EmployeeDashboard";

export default function EmployeePage() {
  // Direct access to employee dashboard - no authentication required
  return <EmployeeDashboard />;
}